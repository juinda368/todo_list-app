name: todo_mobile
description: "待办事项移动端应用"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  dio: ^5.0.3
  shared_preferences: ^2.0.18
  sqflite: ^2.4.2
  intl: ^0.18.1
  image_picker: ^1.0.7
  home_widget: ^0.6.0  # 桌面小组件支持

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
